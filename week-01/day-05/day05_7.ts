'use strict'

for  (let evenNumbers: number = 1; evenNumbers < 100; evenNumbers + 2) {
    console.log (evenNumbers);
}

//Multiplication-table//
let a: number = 15;
let b: number;
for (let c = 1; c <= 10; c++) {
    b = a * c;
    console.log (' ' + a + '*' + c + '=' + b);
}
